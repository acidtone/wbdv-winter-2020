const express = require('express');
const route = express.Router();

// Import model
const Definition = require('../models/Users.js');

// GET /users

// GET /users/:id

// POST /users

// PUT /users/:id

// DELETE /users/:id

module.exports = route;
